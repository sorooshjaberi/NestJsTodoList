import { WithStackFilterTsFilter } from './with-stack.filter.ts.filter';

describe('WithStackFilterTsFilter', () => {
  it('should be defined', () => {
    expect(new WithStackFilterTsFilter()).toBeDefined();
  });
});
